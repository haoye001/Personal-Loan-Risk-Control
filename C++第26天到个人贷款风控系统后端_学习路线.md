# C++ 第26天 → 个人贷款风控系统 C++ 后端学习路线

## 一、学习目标

最终目标：

> 从目前已经学习到面向对象、继承、虚函数、STL、DFS/回溯等内容的阶段，逐步过渡到能够独立完成本项目 C++ 后端核心功能。

最终能够完成：

```text
C++17/20
 ↓
Linux
 ↓
网络基础
 ↓
Drogon
 ↓
HTTP / REST
 ↓
JSON
 ↓
MySQL
 ↓
Controller
 ↓
Service
 ↓
DAO
 ↓
个人贷款业务
 ↓
完整 C++ 后端
```

---

# 二、第一阶段：继续夯实 C++（第26～30天）

目标：不要急着直接学习 Web 框架。

## Day 26：回顾面向对象 + 虚函数

重点：

- 类
- 对象
- 构造函数
- 析构函数
- private / protected / public
- 继承
- 虚函数
- override
- 多态
- this 指针

重点理解：

```cpp
Person* p = new Student();
p->introduce();
```

为什么会调用 Student 的 `introduce()`。

### 练习

设计：

```text
Person
 ├── Student
 └── Employee
```

使用虚函数实现：

```cpp
virtual void introduce();
```

---

# 三、Day 27：STL 深入

重点：

```text
vector
string
map
unordered_map
set
unordered_set
queue
stack
priority_queue
pair
```

同时学习：

- iterator
- range-based for
- lambda
- sort
- find
- lower_bound
- upper_bound

重点理解：

```cpp
vector<int>
map<string, int>
unordered_map<int, User>
```

这些容器以后会大量用于后端。

---

# 四、Day 28：现代 C++

重点：

- auto
- nullptr
- const
- 引用
- 智能指针
- lambda
- enum class
- struct/class
- move
- RAII

尤其重点学习：

```cpp
unique_ptr
shared_ptr
weak_ptr
```

理解：

```text
new/delete
       ↓
智能指针
       ↓
RAII
```

后端项目中尽量避免到处手写：

```cpp
new
delete
```

---

# 五、Day 29：C++ 工程化基础

学习：

- .h / .cpp
- 多文件编译
- namespace
- include
- static
- const
- CMake
- Git

建立第一个：

```text
C++ Project
├── include/
├── src/
├── CMakeLists.txt
└── main.cpp
```

目标：

```bash
cmake
make
```

可以独立编译项目。

---

# 六、Day 30：第一个小型 C++ 项目

建议做：

> 学生信息管理系统

功能：

```text
添加学生
删除学生
修改学生
查询学生
排序
保存
读取
```

数据：

```cpp
struct Student {
    int id;
    string name;
    int age;
    double score;
};
```

这个项目的目的不是功能多，而是练习：

```text
C++
 ↓
类
 ↓
STL
 ↓
多文件
 ↓
CMake
 ↓
Git
```

---

# 七、第二阶段：Linux + 网络基础（第31～35天）

## Day 31：Linux 基础

学习：

```text
pwd
ls
cd
mkdir
cp
mv
rm
cat
grep
find
ps
top
kill
```

同时理解：

```text
进程
线程
端口
IP
文件
权限
```

---

# 八、Day 32：HTTP 基础

必须理解：

```text
客户端
   ↓
HTTP Request
   ↓
服务器
   ↓
HTTP Response
   ↓
客户端
```

HTTP 请求：

```text
POST /api/login HTTP/1.1
Content-Type: application/json
```

JSON：

```json
{
    "username": "Tom",
    "password": "123456"
}
```

响应：

```text
HTTP/1.1 200 OK
```

---

# 九、Day 33：网络编程基础

学习：

```text
Socket
TCP
IP
Port
Client
Server
```

理解最基本的：

```text
Server
 ↓
socket
 ↓
bind
 ↓
listen
 ↓
accept
 ↓
recv
 ↓
send
```

客户端：

```text
socket
 ↓
connect
 ↓
send
 ↓
recv
```

目标不是自己实现 Web 框架，而是理解框架底层到底在做什么。

---

# 十、Day 34：并发基础

学习：

- 进程
- 线程
- mutex
- lock_guard
- condition_variable
- atomic
- thread

重点理解：

```cpp
std::thread
std::mutex
std::lock_guard
```

了解：

```text
多个请求
 ↓
多个线程 / 事件循环
 ↓
共享资源
 ↓
线程安全
```

---

# 十一、Day 35：网络小项目

做一个：

> C++ TCP 聊天程序

实现：

```text
Client A ─┐
Client B ─┼──> Server
Client C ─┘
```

目标：

- socket
- TCP
- 多客户端
- thread
- mutex

完成后再进入 Web 框架。

---

# 十二、第三阶段：Drogon（第36～42天）

## Day 36：了解 Drogon

学习：

- Drogon 是什么
- 项目结构
- Controller
- Route
- Request
- Response
- JSON

理解：

```text
浏览器 / APP
       ↓
HTTP
       ↓
Drogon
       ↓
Controller
```

---

# 十三、Day 37：第一个 API

实现：

```text
GET /hello
```

返回：

```json
{
    "message": "Hello"
}
```

然后实现：

```text
GET /api/test
POST /api/test
```

---

# 十四、Day 38：JSON

重点：

```text
JSON
 ↓
C++
 ↓
对象
```

例如：

```json
{
    "name": "Tom",
    "age": 20
}
```

对应：

```cpp
string name;
int age;
```

实现：

```text
POST /api/user
```

---

# 十五、Day 39：Controller

开始理解：

```text
Controller
```

例如：

```text
UserController
LoanController
RepaymentController
```

目标：

```text
请求
 ↓
Controller
 ↓
返回 JSON
```

---

# 十六、Day 40：Service

开始分层：

```text
Controller
     ↓
Service
     ↓
DAO
```

例如：

```text
LoanController
      ↓
LoanService
      ↓
LoanDAO
      ↓
MySQL
```

理解为什么不能把所有代码全部写在 Controller 中。

---

# 十七、Day 41：MySQL

学习：

```text
数据库
表
字段
主键
外键
索引
事务
```

SQL：

```sql
SELECT
INSERT
UPDATE
DELETE
JOIN
```

同时学习：

```text
C++
 ↓
MySQL
```

如何建立连接和执行查询。

---

# 十八、Day 42：完成 C++ CRUD

完成：

```text
POST   /api/users
GET    /api/users
GET    /api/users/{id}
PUT    /api/users/{id}
DELETE /api/users/{id}
```

做到：

```text
Android / Postman
       ↓
HTTP
       ↓
Drogon
       ↓
Controller
       ↓
Service
       ↓
MySQL
```

这一步完成后，你已经具备真正后端开发的基本能力。

---

# 十九、第四阶段：进入个人贷款项目（第43～50天）

## Day 43：用户模块

实现：

```text
注册
登录
个人信息
```

API：

```text
POST /api/register
POST /api/login
GET /api/users/{id}
PUT /api/users/{id}
```

---

# 二十、Day 44：贷款产品

实现：

```text
贷款产品查询
```

API：

```text
GET /api/loans
```

例如：

```json
{
    "id": 1,
    "name": "个人信用贷",
    "maxAmount": 50000,
    "term": 12
}
```

---

# 二十一、Day 45：贷款申请

实现：

```text
POST /api/loans/apply
```

流程：

```text
用户
 ↓
提交申请
 ↓
Controller
 ↓
LoanService
 ↓
数据库
 ↓
申请成功
```

---

# 二十二、Day 46：贷款状态

设计：

```text
PENDING
REVIEWING
APPROVED
REJECTED
DISBURSED
REPAYING
COMPLETED
```

状态：

```text
待提交
 ↓
审核中
 ↓
通过 / 拒绝
 ↓
放款
 ↓
还款中
 ↓
已结清
```

这一步非常重要，因为它是整个贷款系统业务逻辑的核心。

---

# 二十三、Day 47：审批模块

实现：

```text
GET /api/approval/list
POST /api/approval/{id}/approve
POST /api/approval/{id}/reject
```

业务：

```text
贷款申请
 ↓
审批
 ├── 通过
 └── 拒绝
```

---

# 二十四、Day 48：还款模块

实现：

```text
GET  /api/loans/{id}/repayments
POST /api/repayments
```

业务：

```text
贷款
 ↓
生成还款计划
 ↓
还款
 ↓
剩余金额
 ↓
结清
```

---

# 二十五、Day 49：风控接口

当前阶段不要实现 AI 模型。

只实现：

```text
POST /api/risk/evaluate
```

例如：

```json
{
    "userId": 10001,
    "loanId": 100001
}
```

返回：

```json
{
    "riskScore": 75,
    "riskLevel": "LOW",
    "result": "PASS"
}
```

内部先使用模拟结果。

---

# 二十六、Day 50：完整联调

最终：

```text
Android APP
      ↓
HTTP / JSON
      ↓
C++ / Drogon
      ↓
Controller
      ↓
Service
      ↓
DAO
      ↓
MySQL
```

同时：

```text
后台管理 Web
      ↓
HTTP / JSON
      ↓
C++ / Drogon
```

完成 APP、后台、后端、数据库之间的第一次完整联调。

---

# 二十七、第五阶段：项目能力提升

完成基础功能后再学习：

## 1. 异常处理

```text
参数错误
用户不存在
贷款不存在
数据库异常
重复申请
非法金额
```

统一返回：

```json
{
    "code": 400,
    "message": "贷款金额不合法"
}
```

## 2. 日志

学习：

```text
INFO
WARN
ERROR
```

记录：

```text
用户登录
贷款申请
审批
还款
异常
```

## 3. 安全

至少了解：

- 密码不能明文存储
- 参数校验
- SQL 注入
- Token
- HTTPS
- 权限控制

## 4. 性能

后续再学习：

- 连接池
- 多线程
- 异步
- 缓存
- Redis
- Nginx

这些**不要在项目第一版就全部加入**。

---

# 二十八、你现在最应该学习什么

你目前最重要的不是马上学 Drogon。

建议顺序：

```text
现在
 ↓
Day 26～30
现代 C++ + CMake + Git
 ↓
Day 31～35
Linux + TCP/IP + Socket
 ↓
Day 36～42
Drogon + HTTP + JSON + MySQL
 ↓
Day 43～
正式进入个人贷款后端
```

---

# 二十九、学习原则

## 原则 1：先理解，再使用框架

不要只记：

```cpp
app().addListener(...)
```

而应该知道：

```text
HTTP
 ↓
TCP
 ↓
Socket
 ↓
服务器
```

大概是怎么回事。

## 原则 2：不要重复造轮子

不要为了学习 C++：

```text
自己写 HTTP
自己写 JSON
自己写 ORM
自己写 Web Framework
```

应该：

```text
理解原理
+
使用成熟库
+
完成业务
```

## 原则 3：每学一个知识点都做小实验

例如学习：

```text
unordered_map
```

就做：

```text
用户 ID → 用户信息
```

学习：

```text
thread
```

就做：

```text
多个客户端
```

学习：

```text
JSON
```

就做：

```text
POST /api/user
```

学习：

```text
MySQL
```

就做：

```text
用户 CRUD
```

---

# 三十、最终能力树

完成这条路线后：

```text
                    C++ 后端开发
                         │
        ┌────────────────┼────────────────┐
        │                │                │
      C++              Linux            网络
        │                │                │
   STL/现代C++       进程/线程         TCP/HTTP
        │                │                │
        └────────────────┼────────────────┘
                         │
                      Drogon
                         │
              ┌──────────┼──────────┐
              │          │          │
          Controller  Service      DAO
              │          │          │
              └──────────┼──────────┘
                         │
                       MySQL
                         │
                    贷款业务
                         │
              ┌──────────┼──────────┐
              │          │          │
            用户        贷款        审批
                         │
                       还款
                         │
                       风控
                         │
                    AI模型（后续）
```

---

# 三十一、最重要的最终目标

不是：

> “我学会了 Drogon。”

而是：

> **“我能够用 C++ 独立设计、实现、调试一个具有 Controller、Service、DAO、MySQL 和 REST API 的真实业务后端。”**

这会把你现在的 C++ 学习，从单纯的算法和语法，真正连接到工程开发。
