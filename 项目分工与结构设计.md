# 多数据源融合的互联网个人贷款风控系统

# 项目分工与结构设计（C++ 后端技术栈版）

| 项目 | 内容 |
| :--- | :--- |
| 项目名称 | 多数据源融合的互联网个人贷款风控系统（基本版） |
| 课程名称 | 进阶式挑战性综合项目 I（互联网 + 金融方向） |
| 团队规模 | 3 人 |
| 版本 | V1.1 |
| 日期 | 2026-08-31 |

---

## 一、项目定位

本课题要求设计开发一个多数据源融合的互联网个人贷款风控系统，本阶段（综设 I）完成基础版本的设计与实现，重点覆盖软件工程流程：

- 可行性分析
- 需求分析
- 概要设计
- 详细设计
- 系统架构与模块划分
- 功能设计与接口设计
- 基础代码实现

本阶段系统包含三个主要端：

1. **借款人 Android APP**
2. **借款人 Web 前端（H5 / Web 用户端）**
3. **后台审批管理 Web 系统**

本阶段**不实现真正的机器学习风控模型**，仅预留风控接口，为后续阶段接入 AI 风控模型做准备。

---

## 二、总体系统架构

```text
                    互联网个人贷款风控系统
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
 Android APP            Web 用户端          后台审批管理 Web
 (Kotlin)              (Vue 3)                (Vue 3)
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                         HTTP / JSON
                              │
                              ▼
                    ┌──────────────────┐
                    │ C++ 后端服务器    │
                    │ C++17/20         │
                    │ Drogon           │
                    └────────┬─────────┘
                             │
                    ┌────────┴─────────┐
                    │                  │
               业务 Service          DAO
                    │                  │
                    └────────┬─────────┘
                             │
                             ▼
                          MySQL
```

后续阶段扩展：

```text
贷款申请
   ↓
多数据源
   ↓
数据清洗 → 特征工程 → 机器学习 / 深度学习
   ↓
风险评分 → 自动 / 辅助审批
```

---

## 三、技术栈

| 模块 | 技术 |
| :--- | :--- |
| Android APP | Kotlin + Android Studio |
| Web 用户端 | Vue 3 + Element Plus / Vant |
| 后台管理 Web | Vue 3 + Element Plus |
| 后端 | C++17/20 + Drogon |
| API 风格 | RESTful |
| 数据交换 | JSON |
| 数据库 | MySQL |
| 数据访问 | Drogon ORM / 原生 MySQL |
| API 测试 | Postman |
| 数据库工具 | Navicat / DataGrip |
| UML / 流程图 | draw.io / StarUML |
| 原型 | Figma / Axure |
| 版本控制 | Git + GitHub/Gitee |
| 服务器环境 | Linux / Ubuntu |

---

## 四、三人分工

### 成员 A：Android APP 开发负责人

负责借款人 Android APP。

主要模块：

- 用户注册 / 登录
- 首页
- 个人信息管理
- 贷款产品展示
- 贷款申请
- 我的贷款（申请记录 / 进度）
- 还款记录
- 消息 / 状态通知

APP 核心业务流程：

```text
注册 → 登录 → 填写个人信息 → 查看贷款产品 → 提交贷款申请
 → 审核中 → 通过 / 拒绝 → 放款 → 还款 → 结清
```

学习重点：

```text
Kotlin 基础 → Android Studio → Activity / Fragment → XML / UI
 → RecyclerView → HTTP → JSON → OkHttp → 调用 C++ REST API
```

主要交付物：

- Android App 页面与跳转
- 后端 API 调用与 JSON 解析
- 注册 / 登录功能
- 贷款申请功能
- 我的贷款功能

---

### 成员 B：C++ 后端开发负责人

负责整个 C++ 后端服务器。

核心技术路线：

```text
C++17/20 → Drogon → HTTP → RESTful API → JSON → Service → DAO → MySQL
```

后端模块：

```text
backend/
├── controller/
│   ├── UserController
│   ├── LoanController
│   ├── RepaymentController
│   ├── ApprovalController
│   └── RiskController
├── service/
│   ├── UserService
│   ├── LoanService
│   ├── RepaymentService
│   ├── ApprovalService
│   └── RiskService
├── model/
│   ├── User
│   ├── Loan
│   ├── Repayment
│   └── Approval
├── dao/
│   └── Database
├── common/
│   ├── Result
│   ├── Exception
│   └── Utils
└── main.cc
```

Controller 层职责：

```text
接收 HTTP 请求 → 解析 JSON → 参数校验 → 调用 Service → 返回 JSON
```

Service 层职责：

```text
贷款申请 → 检查用户 → 检查贷款产品 → 检查申请额度
 → 创建贷款申请 → 保存数据库
```

DAO 层职责：

- 数据库连接
- SQL 查询、插入、更新、删除

主要交付物：

- C++ Drogon 后端服务
- RESTful API 实现
- 业务逻辑处理
- 数据库访问层
- 统一返回结果封装

---

### 成员 C：Web 前端 + 数据库 + 测试 + 金融业务负责人

成员 C 负责所有 Web 前端（用户端 + 管理端）、数据库、测试与金融业务梳理。

#### 1. Web 前端

Web 前端分为两个子系统：

| 子系统 | 技术栈 | 主要用户 | 功能 |
| :--- | :--- | :--- | :--- |
| Web 用户端 | Vue 3 + Vant / Element Plus | 借款用户 | 注册登录、产品展示、贷款申请、进度查询、还款计划 |
| 后台管理 Web | Vue 3 + Element Plus | 审批人员 / 管理员 | 登录、审批、用户管理、数据统计 |

Web 用户端页面：

- 登录 / 注册页
- 首页（额度展示、产品入口）
- 贷款产品页
- 贷款申请页
- 我的贷款页
- 还款计划页
- 个人中心页

后台管理端页面：

- 管理员登录
- 用户管理
- 贷款申请查看
- 贷款审批
- 审批记录
- 还款信息
- 数据统计

#### 2. 数据库

建议核心表：

```text
user
user_profile
loan_product
loan
loan_application
approval
approval_record
repayment
repayment_record
risk_assessment
operation_log
admin
```

#### 3. E-R 图设计

```text
User
 │
 ├── UserProfile
 │
 ├── LoanApplication
 │       │
 │       └── Loan
 │              │
 │              └── Repayment
 │
 └── RiskAssessment
```

#### 4. 测试

负责：

- 测试用例
- API 测试
- 数据库测试
- 功能测试
- 异常测试
- 集成测试
- 系统测试

#### 5. 金融业务梳理

```text
用户 → 贷款产品 → 贷款申请 → 风险评估 → 人工审批
 → 通过 / 拒绝 → 放款 → 还款 → 结清
```

主要交付物：

- Web 用户端页面与功能
- Web 管理端页面与功能
- 数据库 E-R 图与建表 SQL
- 测试用例与测试报告
- 金融业务规则说明

---

## 五、三人共同负责的内容

不能把项目完全割裂成三块，三人需要共同参与以下内容：

### 1. 可行性分析

共同对比以下后端方案：

```text
Java / Spring Boot
C++ / Drogon
Python / Django
Node.js / Express
```

最终选择 **C++ / Drogon** 时说明：

- 与小组已有 C++ 学习基础结合
- 满足 HTTP / REST API 需求
- 能够连接 MySQL
- 能满足当前项目规模
- 后续可以扩展

同时承认风险：

- C++ Web 生态和开发效率相比 Java 不占优势
- 团队需要额外学习 Drogon
- 因此需要控制系统规模，避免过度工程化

### 2. 需求分析

共同确认：

- 用户需求（Android + Web 用户端）
- 管理员需求
- 贷款业务流程
- 审批业务流程
- 还款业务流程
- 风控接口需求

### 3. 概要设计

共同讨论：

```text
系统架构 → 模块划分 → 技术选型 → 数据流 → 接口 → 安全 → 扩展性
```

### 4. 详细设计

共同完成：

- 类设计
- API 文档
- 数据库设计
- 页面设计（Android + Web 两端）
- 业务流程
- 异常处理

### 5. 最终报告与答辩

三人都必须能够解释：

```text
为什么这样设计？
为什么选择 C++？
C++ 后端如何处理 HTTP？
APP 和 Web 如何调用后端？
数据库如何设计？
风控接口如何预留？
系统未来如何接入 AI？
```

---

## 六、核心 API 概览

### 用户

```text
POST /api/register
POST /api/login
GET  /api/users/{id}
PUT  /api/users/{id}
```

### 贷款产品

```text
GET  /api/loans
```

### 贷款

```text
GET  /api/loans
POST /api/loans/apply
GET  /api/loans/{id}
GET  /api/users/{id}/loans
```

### 还款

```text
GET  /api/repayments
POST /api/repayments
GET  /api/loans/{id}/repayments
```

### 审批

```text
GET  /api/approval/list
GET  /api/approval/{id}
POST /api/approval/{id}/approve
POST /api/approval/{id}/reject
```

### 风控（本阶段仅预留接口）

```text
POST /api/risk/evaluate
```

请求示例：

```json
{
    "userId": 10001,
    "loanId": 100001
}
```

响应示例：

```json
{
    "riskScore": 75,
    "riskLevel": "LOW",
    "result": "PASS"
}
```

后续可扩展为：

```text
C++ Backend → RiskService → AI Risk Model → riskScore
```

无需改变 APP 和 Web 系统的主要接口。

---

## 七、项目目录结构

```text
personal-loan-risk-system/
│
├── android-app/                    # Android 借款端（成员 A）
│   ├── app/src/main/java/com/example/loan/
│   ├── app/src/main/res/
│   └── build.gradle
│
├── backend/                        # C++ 后端服务（成员 B）
│   ├── controller/
│   ├── service/
│   ├── model/
│   ├── dao/
│   ├── common/
│   ├── config.json
│   ├── CMakeLists.txt
│   └── main.cc
│
├── web-user/                       # Web 用户端（成员 C）
│   ├── src/
│   ├── public/
│   └── package.json
│
├── admin-web/                      # 后台管理 Web 端（成员 C）
│   ├── src/
│   ├── public/
│   └── package.json
│
├── database/                       # 数据库脚本（成员 C）
│   ├── create.sql
│   └── test-data.sql
│
├── docs/                           # 软件开发文档（三人协作）
│   ├── 01-可行性分析报告.md
│   ├── 02-需求规格说明书.md
│   ├── 03-概要设计说明书.md
│   ├── 04-详细设计说明书.md
│   ├── API接口文档.md
│   ├── 数据库设计.md
│   └── 测试报告.md
│
├── README.md
└── .gitignore
```

---

## 八、6 周项目安排

| 周次 | 成员 A | 成员 B | 成员 C | 共同输出 |
| :--- | :--- | :--- | :--- | :--- |
| 第 1 周 | APP 技术调研 | C++/Drogon 调研 | Web 前端 / 金融业务 / DB 调研 | 可行性分析报告 |
| 第 2 周 | APP 需求 | API / 后端需求 | Web 两端 / 业务 / DB 需求 | 需求规格说明书 |
| 第 3 周 | APP 架构 | 后端架构 | Web 架构 / E-R / 数据库设计 | 概要设计说明书 |
| 第 4 周 | UI / 页面详细设计 | API / Service 详细设计 | Web 页面 / DB / 测试详细设计 | 详细设计说明书 |
| 第 5 周 | APP 实现 | C++ 后端实现 | Web 用户端 / 管理端 / DB 实现 | 第一轮联调 |
| 第 6 周 | APP 完善 | 后端完善 | Web 完善 / 测试完善 | 集成、报告、答辩 |

---

## 九、Git 协作规范

推荐分支模型：

```text
main
 │
 └── develop
      ├── feature/android
      ├── feature/backend
      ├── feature/web-user
      └── feature/admin-web
```

规则：

- 不直接修改 `main`
- 每个人使用自己的 feature 分支
- 功能完成后合并到 `develop`
- 定期进行多端联调（Android + Web 用户端 + Web 管理端 + 后端）
- commit 信息清晰

示例：

```text
feat: add loan application API
feat: add Android login page
feat: add web user login page
feat: add loan database tables
fix: validate loan amount
```

---

## 十、最终建议

本项目采用 C++ 后端是可行的，但要坚持：

> **使用成熟框架，而不是自己造 Web 框架。**

推荐路线：

```text
C++17/20 → 现代 C++ → Linux → 网络基础 → Drogon → REST API → MySQL → 个人贷款业务 → 完整后端
```

核心目标不是“证明 C++ 能做 Web”，而是：

> **利用 C++ 学习基础完成一个真正可以运行、可以联调、可以扩展的后端系统。**
